// Compression: String -> Base64
export const p_compressToBase64 = async (input: string): Promise<string> => {
    return input;
    };
    
    // Decompression: Base64 -> String
export const p_decompressFromBase64 = async (b64String: string): Promise<string> => {
    return b64String;
    };